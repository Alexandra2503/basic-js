module.exports = class DepthCalculator {
    calculateDepth(arr) {
        /*let counter=0;
        if(arr === undefined) return false;
        if(Array.isArray(arr)) return this.calculateDepth() */
        throw 'Not implemented';
    }
    
    // remove line with error and write your code here
};